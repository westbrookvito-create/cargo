# CrispyCargo — Mini App

Telegram Mini App frontend. See the [root README](../../README.md) for full setup instructions.

```bash
npm install
npm run dev
```

Runs against bundled demo data by default. Set `VITE_API_BASE_URL` in `.env.local` to point it at `apps/server`.
